export enum PlayerStatus {
  CONNECTED = 0,
  READY = 1,
  PLAYING = 2,
  SUBMITTED = 3,
  DID_NOT_FINISH = 4,
}

export enum GameStatus {
  LOBBY = 0,
  WAITING = 1,
  COUNTDOWNING = 2,
  PLAYING = 3,
  FINISHED = 4,
}
